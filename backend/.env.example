# Environment variables for Nutri 4.0 Backend
# Copy this file to .env and update the values

# Django settings
DJANGO_ENV=development
DJANGO_SECRET_KEY=your-secret-key-here-with-at-least-50-characters-and-good-randomness
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings
DB_NAME=nutrixpert_db
DB_USER=root
DB_PASSWORD=NutriXpert2024
DB_HOST=localhost
DB_PORT=3306

# Redis settings (if needed)
REDIS_URL=redis://localhost:6379/1

# JWT settings
SIMPLE_JWT_SECRET_KEY=your-jwt-secret-key

# Email settings (if needed)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-email-password

# Google OAuth (if needed)
GOOGLE_OAUTH2_CLIENT_ID=your-google-client-id